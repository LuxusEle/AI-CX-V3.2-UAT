apiVersion: v1
kind: Service
metadata:
  name: nexus-api-service
  labels:
    app: nexus-api
spec:
  selector:
    app: nexus-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
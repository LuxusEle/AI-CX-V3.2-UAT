# Global (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nexus
POSTGRES_PORT=5432
REDIS_PORT=6379

# For S3-compatible storage (optional, used in API service stubs)
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=nexus-dev
S3_ACCESS_KEY=minio
S3_SECRET_KEY=miniosecret

# UAT / PROD toggles
UAT_MODE=true
PRODUCTION_HARDEN=false
